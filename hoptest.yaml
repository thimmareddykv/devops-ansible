---
# file: devops-ansible/hoptest.yaml
# Examples:
#   ansible-playbook hoptest.yaml -i hosts/hoptest.ini
#   (SUMO) ansible-playbook hoptest.yaml -i hosts/hoptest.ini

- hosts: hoptest

  roles:
    - { role: common, tags: "common" }
#    - { role: jumpserver, tags: "jumpserver" }
    - { role: sshd, tags: "sshd" }
#    - { role: sumologic, tags: "sumologic" }
#    - { role: papertrail, tags: "papertrail" }
  sudo: yes
  vars:

